# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Virtualenvs
.venv/
venv/
env/

# Environment / secrets
.env

# TLS material (self-signed certs are generated per host at runtime)
certs/
*.crt
*.key
*.pem

# Runtime logs
logs/
*.log

# IDE / tooling
.idea/
.vscode/
.claude/

# OS
.DS_Store
Thumbs.db

# Office lock files (open documents)
~$*

# Build / deploy artifacts
*.tar
*.tar.gz
dist/
build/

exports/

# Remembered SSH credentials (encrypted) + key
data/

# Update agent scratch (host<->container handoff: state, job, request files)
.update/
# Shared with deploy/host_agent.py — request/result files, never source.
.hostexec/

# Generated from Radware's internal support knowledge base — internal-only
# content, and the GitHub remote for this repo is PUBLIC. Never commit.
.github/system_operations.md
