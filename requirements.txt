fastapi>=0.100.0
uvicorn>=0.20.0
requests>=2.28.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
paramiko>=3.0.0
python-multipart>=0.0.6
cryptography>=41.0.0
# MariaDB driver (modules/maria). PyMySQL is pure Python on purpose: mysqlclient
# and mariadb need a C toolchain and libmariadb headers at build time, which the
# CC's CI/CD base-image rules are unlikely to welcome in a slim image.
PyMySQL>=1.1.0
# PostgreSQL driver (modules/pg). pg8000 for the same reason as PyMySQL above:
# psycopg2 needs libpq headers and a C build step; pg8000 is pure Python.
pg8000>=1.31.0

